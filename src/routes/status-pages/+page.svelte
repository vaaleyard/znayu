<script lang="ts">
  import { onMount } from 'svelte';

  type StatusPage = { name: string; slug: string; statusSlug: string; services: number; uptime: string; state: 'operational' | 'degraded' };

  const statusPages: StatusPage[] = [
    { name: 'Acme Cloud', slug: 'status.acme.cloud', statusSlug: 'acme-cloud', services: 8, uptime: '99.98%', state: 'operational' },
    { name: 'Internal Platform', slug: 'platform.internal', statusSlug: 'internal-platform', services: 12, uptime: '99.94%', state: 'degraded' }
  ];

  let sidebarCollapsed = false;

  onMount(() => {
    sidebarCollapsed = localStorage.getItem('znayu.sidebar.collapsed') === 'true';
  });

  function toggleSidebar() {
    sidebarCollapsed = !sidebarCollapsed;
    localStorage.setItem('znayu.sidebar.collapsed', String(sidebarCollapsed));
  }
</script>

<svelte:head>
  <title>Status pages — Znayu</title>
  <meta name="description" content="Review public status pages in the Znayu admin console." />
</svelte:head>

<main class:sidebar-collapsed={sidebarCollapsed} class="status-pages-page">
  <aside class="sidebar" aria-label="Operator navigation">
    <div class="sidebar-top"><a class="wordmark" href="/dashboard" aria-label="Znayu operator dashboard">znayu</a><span class="console-label">Console</span><button class="sidebar-toggle" type="button" aria-label={sidebarCollapsed ? 'Expand sidebar' : 'Collapse sidebar'} title={sidebarCollapsed ? 'Expand sidebar' : 'Collapse sidebar'} onclick={toggleSidebar}><svg aria-hidden="true" viewBox="0 0 20 20"><rect x="3.5" y="3.5" width="13" height="13" rx="2" /><path d="M8 3.5v13" />{#if sidebarCollapsed}<path d="m10.5 7 3 3-3 3" />{:else}<path d="m12.5 7-3 3 3 3" />{/if}</svg></button></div>
    <div class="create-actions"><a class="sidebar-create" href="/monitors/new"><span aria-hidden="true">+</span> Create monitor</a><a class="sidebar-create secondary" href="/status-pages/new">Create status page</a></div>
    <nav class="sidebar-nav" aria-label="Console sections">
      <a href="/dashboard"><svg aria-hidden="true" viewBox="0 0 20 20"><path d="M3 10h5V3H3v7Zm9 7h5v-7h-5v7ZM3 17h5v-4H3v4Zm9-14v4h5V3h-5Z" /></svg> Overview</a>
      <a class="active" href="/status-pages" aria-current="page"><svg aria-hidden="true" viewBox="0 0 20 20"><path d="M3 4.5h14v11H3v-11Zm3 3h8M6 11h5" /></svg> Status pages</a>
      <a href="/monitors"><svg aria-hidden="true" viewBox="0 0 20 20"><path d="M3 15.5V4.5h14v11H3Zm3-3 2-2 2 1.5 3-4 2 2.5" /></svg> Monitors</a>
      <a href="/activity"><svg aria-hidden="true" viewBox="0 0 20 20"><path d="M10 3v7l4 2M17 10a7 7 0 1 1-14 0 7 7 0 0 1 14 0Z" /></svg> Activity</a>
    </nav>
    <div class="sidebar-bottom"><div class="operator"><span class="operator-avatar" aria-hidden="true">JS</span><span><strong>Jordan Smith</strong><small>Owner</small></span><button type="button">More</button></div></div>
  </aside>

  <section class="status-pages-content" aria-labelledby="status-pages-title">
    <header class="content-header"><div><p class="section-kicker">Admin console</p><h1 id="status-pages-title">Status pages</h1></div><div class="header-actions"><label class="search-wrap"><span class="search-icon" aria-hidden="true"></span><span class="sr-only">Search status pages</span><input type="search" placeholder="Search status pages" /></label><a class="create-button" href="/status-pages/new">Create status page</a></div></header>
    <section class="status-pages-register" aria-labelledby="register-title">
      <header class="register-header"><h2 id="register-title"><svg aria-hidden="true" viewBox="0 0 16 16"><path d="m3 6 5 5 5-5" /></svg> Status pages</h2><span>{statusPages.length} configured</span></header>
      {#each statusPages as page}
        <article class="status-page-row"><span class="state-dot {page.state}" aria-label={page.state}></span><div class="page-identity"><h3><a href={`/status-pages/new?edit=${page.statusSlug}`}>{page.name}</a> <a class="external-arrow-link" href={`/status/${page.statusSlug}`} aria-label={`Open public status page for ${page.name}`}><svg class="external-arrow" aria-hidden="true" viewBox="0 0 16 16" fill="none" stroke="currentColor" stroke-linecap="round" stroke-linejoin="round" stroke-width="1.25"><path d="M4 12 12 4M6 4h6v6" /></svg></a></h3><p>{page.slug}</p></div><div class="page-state {page.state}"><span></span>{page.state === 'operational' ? 'Operational' : 'Degraded'}</div><div class="page-stat"><strong>{page.uptime}</strong><span>{page.services} services</span></div><button class="row-menu" type="button" aria-label={`Actions for ${page.name}`}>More</button></article>
      {/each}
    </section>
  </section>
</main>

<style>
  .status-pages-page { min-height: 100svh; display: grid; grid-template-columns: 232px minmax(0, 1fr); background: var(--page); color: var(--ink); }
  .sidebar { position: sticky; top: 0; display: flex; height: 100svh; min-height: 100svh; padding: 22px 14px 16px; flex-direction: column; background: #141821; border-right: 1px solid var(--border); }.sidebar-top { display: flex; align-items: baseline; justify-content: space-between; padding: 0 10px; }.wordmark { color: var(--ink); font-size: 1.65rem; font-weight: 720; letter-spacing: -0.045em; line-height: 1; text-decoration: none; }.console-label, .section-kicker, .operator small { color: var(--muted); font-size: 0.6875rem; line-height: 1.4; }.console-label, .section-kicker { letter-spacing: 0.02em; text-transform: uppercase; }.create-actions { display: grid; gap: 8px; margin: 38px 0 28px; }.sidebar-create { display: flex; align-items: center; justify-content: center; gap: 7px; min-height: 38px; border: 1px solid var(--green); border-radius: 5px; color: var(--page); background: var(--green); font-size: 0.8125rem; font-weight: 620; text-decoration: none; }.sidebar-create.secondary { border-color: var(--border-light); color: var(--secondary); background: transparent; }.sidebar-nav { display: grid; gap: 3px; }.sidebar-nav a { display: flex; align-items: center; gap: 11px; padding: 10px; border-radius: 5px; color: var(--secondary); font-size: 0.8125rem; text-decoration: none; }.sidebar-nav a:hover, .sidebar-nav a.active { color: var(--ink); background: var(--surface-hover); }.sidebar-nav svg { display: block; width: 16px; height: 16px; flex: 0 0 auto; fill: none; stroke: currentColor; stroke-linecap: round; stroke-linejoin: round; stroke-width: 1.5; }.sidebar-nav a:first-child svg { fill: currentColor; stroke: none; }.sidebar-bottom { display: grid; gap: 20px; margin-top: auto; }.public-link { padding: 0 10px; color: var(--secondary); font-size: 0.6875rem; text-underline-offset: 3px; }.operator { display: flex; align-items: center; gap: 9px; padding: 12px 8px 0; border-top: 1px solid var(--border); }.operator-avatar { display: grid; width: 28px; height: 28px; place-items: center; border-radius: 50%; color: var(--page); background: var(--green); font-size: 0.6875rem; font-weight: 700; }.operator > span:nth-child(2) { display: grid; min-width: 0; gap: 2px; }.operator strong { overflow: hidden; color: var(--secondary); font-size: 0.6875rem; font-weight: 600; text-overflow: ellipsis; white-space: nowrap; }.operator button { margin-left: auto; border: 0; color: var(--muted); background: transparent; font-size: 0.6875rem; cursor: pointer; }.sidebar-toggle { display: grid; width: 24px; height: 24px; flex: 0 0 auto; place-items: center; padding: 0; border: 1px solid var(--border-light); border-radius: 5px; color: var(--muted); background: transparent; cursor: pointer; }.sidebar-toggle svg { width: 14px; height: 14px; fill: none; stroke: currentColor; stroke-linecap: round; stroke-linejoin: round; stroke-width: 1.5; }
  .sidebar-collapsed.status-pages-page { grid-template-columns: 72px minmax(0, 1fr); }.sidebar-collapsed .sidebar { padding-right: 10px; padding-left: 10px; }.sidebar-collapsed .sidebar-top { align-items: center; flex-direction: column; gap: 12px; padding: 0; }.sidebar-collapsed .wordmark { font-size: 0; }.sidebar-collapsed .wordmark::after { color: var(--ink); content: 'z'; font-size: 1.65rem; font-weight: 720; }.sidebar-collapsed .console-label { display: none; }.sidebar-collapsed .create-actions { justify-items: center; }.sidebar-collapsed .sidebar-create { width: 40px; padding: 0; font-size: 0; gap: 0; }.sidebar-collapsed .sidebar-create span { display: block; position: relative; width: 14px; height: 14px; font-size: 0; }.sidebar-collapsed .sidebar-create span::before, .sidebar-collapsed .sidebar-create span::after { position: absolute; top: 6px; left: 1px; width: 12px; height: 2px; border-radius: 1px; background: currentColor; content: ''; }.sidebar-collapsed .sidebar-create span::after { transform: rotate(90deg); }.sidebar-collapsed .sidebar-create.secondary { display: none; }.sidebar-collapsed .sidebar-nav a { justify-content: center; gap: 0; padding-right: 10px; padding-left: 10px; font-size: 0; }.sidebar-collapsed .sidebar-nav svg { width: 18px; height: 18px; }.sidebar-collapsed .public-link { display: none; }.sidebar-collapsed .operator { justify-content: center; padding-right: 0; padding-left: 0; }.sidebar-collapsed .operator > span:nth-child(2), .sidebar-collapsed .operator button { display: none; }
  .status-pages-content { width: min(100%, 1080px); margin: 0 auto; padding: 84px 44px 64px; }.content-header { display: flex; align-items: flex-end; justify-content: space-between; gap: 24px; margin-bottom: 32px; }.section-kicker { margin: 0 0 7px; }h1, h2, h3, p { margin: 0; }h1 { font-size: 1.65rem; font-weight: 680; letter-spacing: -0.03em; line-height: 1.25; }.header-actions { display: flex; align-items: center; gap: 10px; }.search-wrap { position: relative; display: block; width: 280px; }.search-wrap input { width: 100%; height: 42px; padding: 0 12px 0 36px; border: 1px solid var(--border-light); border-radius: 5px; color: var(--ink); background: var(--surface); font: inherit; font-size: 0.8125rem; }.search-wrap input::placeholder { color: var(--muted); }.search-icon { position: absolute; top: 13px; left: 13px; width: 13px; height: 13px; border: 2px solid var(--muted); border-radius: 50%; }.search-icon::after { position: absolute; right: -5px; bottom: -3px; width: 5px; height: 2px; background: var(--muted); content: ''; transform: rotate(45deg); }.create-button { display: inline-flex; min-height: 42px; align-items: center; padding: 0 17px; border: 1px solid var(--green); border-radius: 5px; color: var(--page); background: var(--green); font-size: 0.8125rem; font-weight: 650; text-decoration: none; white-space: nowrap; }.status-pages-register { overflow: hidden; border: 1px solid var(--border); border-radius: 12px; background: var(--surface); }.register-header { display: flex; align-items: center; justify-content: space-between; min-height: 54px; padding: 0 20px; border-bottom: 1px solid var(--border); }.register-header h2 { display: flex; align-items: center; gap: 6px; color: var(--secondary); font-size: 0.8125rem; font-weight: 600; }.register-header h2 svg { width: 14px; height: 14px; fill: none; stroke: var(--muted); stroke-linecap: round; stroke-linejoin: round; stroke-width: 1.5; }.register-header > span { color: var(--muted); font-size: 0.6875rem; }.status-page-row { display: grid; grid-template-columns: 14px minmax(0, 1fr) auto auto 42px; align-items: center; gap: 12px; min-height: 74px; padding: 12px 20px; }.status-page-row + .status-page-row { border-top: 1px solid var(--border); }.state-dot { width: 8px; height: 8px; border-radius: 50%; background: var(--green); }.state-dot.degraded { background: var(--red); }.page-identity { min-width: 0; }.page-identity h3 { overflow: hidden; font-size: 0.8125rem; font-weight: 620; line-height: 1.4; text-overflow: ellipsis; white-space: nowrap; }.page-identity h3 a { color: inherit; text-decoration: none; }.page-identity h3 a:hover { text-decoration: underline; text-underline-offset: 3px; }.page-identity p { overflow: hidden; margin-top: 3px; color: var(--muted); font-size: 0.8125rem; text-overflow: ellipsis; white-space: nowrap; }.page-state { display: flex; align-items: center; gap: 6px; color: var(--green); font-size: 0.6875rem; font-weight: 600; white-space: nowrap; }.page-state.degraded { color: var(--red); }.page-state span { width: 6px; height: 6px; border-radius: 50%; background: currentColor; }.page-stat { display: grid; min-width: 76px; gap: 2px; text-align: right; }.page-stat strong { font-size: 0.8125rem; font-weight: 620; }.page-stat span { color: var(--muted); font-size: 0.6875rem; }.row-menu { border: 0; color: var(--muted); background: transparent; font-size: 0.6875rem; cursor: pointer; }.row-menu:hover { color: var(--ink); }.sr-only { position: absolute; width: 1px; height: 1px; overflow: hidden; clip: rect(0 0 0 0); white-space: nowrap; }
  :focus-visible { outline: 2px solid var(--green); outline-offset: 3px; }
  @media (max-width: 860px) { .status-pages-content { padding: 48px 28px 48px; }.content-header { align-items: flex-start; flex-direction: column; }.header-actions { width: 100%; }.search-wrap { flex: 1; width: auto; } }
  @media (max-width: 680px) { .status-pages-page { display: block; }.sidebar { position: static; height: auto; min-height: auto; padding: 14px 12px 12px; border-right: 0; border-bottom: 1px solid var(--border); }.sidebar-top { padding: 0 4px; }.sidebar-toggle { display: none; }.create-actions { margin: 22px 0 14px; }.sidebar-nav { display: flex; overflow-x: auto; }.sidebar-nav a { flex: 0 0 auto; }.sidebar-bottom { display: none; }.status-pages-content { padding: 28px 14px 40px; }.header-actions { align-items: stretch; flex-direction: column; }.search-wrap { width: 100%; }.create-button { justify-content: center; }.status-page-row { grid-template-columns: 10px minmax(0, 1fr) 42px; }.page-state, .page-stat { display: none; } }
  .external-arrow-link { display: inline-flex; color: var(--muted); text-decoration: none; vertical-align: -1px; }.external-arrow-link:hover { color: var(--ink); }.external-arrow { display: inline-block; width: 12px; height: 12px; margin-left: 3px; fill: none; stroke: currentColor; stroke-linecap: round; stroke-linejoin: round; stroke-width: 1.25; vertical-align: -1px; }
</style>
